const videoReducer = (state, action) => {

    switch(action.type){

        default:
            return state
    }
}

const initialVideoState = {
    like : [],
    playlist : [],
    bookmark : []
}

export {
    videoReducer,
    initialVideoState
}
